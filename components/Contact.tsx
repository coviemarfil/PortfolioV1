import { Button } from "./Button";
import { SectionHeading } from "./SectionHeading";
import { socials } from "@/lib/portfolio";

export function Contact() { return <section id="contact" className="py-20 sm:py-28 lg:py-32"><div className="mx-auto w-[min(100%-2rem,72rem)]"><SectionHeading id="contact-heading" label="06 / Contact" title="Let&apos;s connect" description="Open to entry-level developer and IT roles. References available upon request." /><div className="mt-10 grid gap-10 md:grid-cols-2 md:gap-12"><dl className="space-y-4">{socials.map((social) => <div key={social.label} className="rounded-xl border border-border bg-surface p-5"><dt className="font-mono text-xs uppercase tracking-[0.1em] text-subtle">{social.label}</dt><dd className="mt-1 break-words text-sm text-foreground"><a href={social.href} target={social.external ? "_blank" : undefined} rel={social.external ? "noreferrer" : undefined} className="hover:text-muted">{social.value}</a></dd></div>)}</dl><div><p className="max-w-xl text-muted">I&apos;m actively looking for a development team where I can contribute from the start and keep building my technical skills. If that sounds like a fit, I&apos;d love to hear from you.</p><div className="mt-7"><Button href="mailto:coviemarfil1@gmail.com?subject=Portfolio%20Inquiry">Send an email</Button></div></div></div></div></section>; }
